# Credentials for the Postgres in docker-compose.yml (local image checks only).
# The backend service's DATABASE_URL override must match these.
#   cp database.env.example database.env
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=tmx_hr
